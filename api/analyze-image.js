import OpenAI from 'openai';export default async function handler(req,res){if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:'AI is nog niet geconfigureerd.'});const {image,context}=req.body||{};if(!image)return res.status(400).json({error:'Geen foto.'});try{const c=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await c.responses.create({model:process.env.OPENAI_MODEL||'gpt-5.6',input:[{role:'user',content:[{type:'input_text',text:`Je bent ADAS Expert. Analyseer deze foto voor een monteur. Max 80 woorden. Gebruik EXACT: ZICHTBAAR: ... MOGELIJK RELEVANT: ... CONTROLEER: ... Geen verzonnen meetwaarden. Context:${context||'geen'}`},{type:'input_image',image_url:image,detail:'high'}]}]});res.json({answer:r.output_text||'Geen analyse.'})}catch(e){res.status(502).json({error:'Fotoanalyse mislukt: '+e.message})}}