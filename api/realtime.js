export default async function handler(req,res){if(!process.env.OPENAI_API_KEY)return res.status(503).send('AI is nog niet geconfigureerd.');const {sdp}=req.body||{};if(!sdp)return res.status(400).send('SDP ontbreekt.');try{const form=new FormData();form.append('sdp',sdp);form.append('session',JSON.stringify({type:'realtime',model:process.env.REALTIME_MODEL||'gpt-realtime',audio:{input:{turn_detection:{type:'server_vad'}},output:{voice:'marin'}},instructions:'Je bent ADAS Expert, een ervaren Nederlandse ADAS-monteur. Praat natuurlijk in het Nederlands. Houd antwoorden kort en praktisch. Stel gerichte vragen. Verzin geen fabrikant-specifieke waarden; fabrikantprocedure is leidend.'}));const r=await fetch('https://api.openai.com/v1/realtime/calls',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:form});const t=await r.text();res.status(r.status).type('application/sdp').send(t)}catch(e){res.status(502).send('Realtime fout: '+e.message)}}